Dockerized pure-ftpd server
============================
forked from stilliard/docker-pure-ftpd

----------------------------------------

It is assumed that you build this locally. E.g.:
```bash
docker build -t local/pure-ftpd .
```
